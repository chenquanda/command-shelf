# CommandShelf

CommandShelf 是一个面向个人使用的 Windows 常用命令看板，用来保存 Linux、Docker、Git、SSH 等容易忘记的命令。每条记录可以包含命令说明、用法、参数说明和参考输出。

应用采用本地优先设计：命令数据保存在用户选择的 Git 仓库中，浏览和编辑不依赖网络；需要在多台电脑间同步时，由用户明确点击“拉取”或“推送”。

## 主要功能

- 按分类浏览、新增和编辑命令。
- 复制命令正文或参考输出；正式命令正文复制成功后原位更新并保存累计次数，不重建命令列表。
- 使用拖动或 `Alt+↑`、`Alt+↓` 调整命令顺序。
- 用户点击“按次数排序”后，按复制次数从高到低整理当前分类；应用不会自动重排。
- 通过本机 Codex CLI 把自然语言问题生成结构化命令草稿；草稿先临时展示，由用户确认后才保存。
- 通过系统 Git 快进拉取和普通推送 `commands.json`；Git 等待在后台线程执行，窗口仍可拖动和重绘。
- 在同步失败时保留本地数据，不自动合并冲突或强制覆盖远端。
- 应用绝不执行 Codex 生成的命令；连续两次格式不合格时保留原文供人工整理。

## 项目结构

- `frontend/index.html`：正式 Tauri 桌面前端。
- `src-tauri`：Rust 后端、受控 Codex CLI 调用、数据持久化、备份、Git 同步和 Windows 打包配置。
- `scripts/desktop-smoke.mjs`：真实桌面入口的自动验收脚本。
- `scripts/release-candidate.ps1`：测试、静态检查和 Windows 安装包构建脚本。
- `docs/安装与使用说明.md`：安装、数据仓库配置和日常使用说明。
- `release`：随源码仓库保存的 Windows 安装包。

## 开发与验证

开发机需要 Rust、Tauri CLI、MSVC Build Tools、WebView2 和 Git for Windows。使用 AI 草稿时，还需要系统 PATH 中存在已完成登录的 Codex CLI。前端不需要 Node 构建；Node 仅供桌面验收脚本和前端语法检查使用。

```powershell
cargo fmt --manifest-path src-tauri\Cargo.toml --check
cargo test --manifest-path src-tauri\Cargo.toml --all-targets
cargo clippy --manifest-path src-tauri\Cargo.toml --all-targets -- -D warnings
pwsh -ExecutionPolicy Bypass -File scripts\release-candidate.ps1
```

发布脚本会在项目 `.local` 下重新创建编译缓存、运行期临时目录、发布产物和验证证据。这些内容均被 Git 忽略，可以在构建完成后清理。

## 数据与同步约束

- 数据仓库只需要维护一个 `commands.json`，当前格式版本为 `schemaVersion: 1`。
- 命令的 `copyCount` 保存正文复制次数；旧数据没有该字段时自动按 `0` 读取。
- 应用只管理 `commands.json`，不会暂存或提交仓库中的其他文件。
- GitHub 程序仓库：`git@github.com:chenquanda/command-shelf.git`。
- GitHub 数据仓库：`git@github.com:chenquanda/command-shelf-data.git`。
- 应用不内置 GitHub 登录、Token 管理、后台同步或冲突解决界面。
- Codex 草稿在点击“保存到当前分类”前只存在于内存，不写入数据仓库，也不会让 Git 状态变脏。
- 卸载应用不会删除用户选择的数据仓库。

## 当前发布状态

- Rust 49 项测试、Clippy 零警告、格式检查、前端语法和 Windows x64 NSIS 发布构建均已通过。
- 当前安装包 SHA-256：`CD14AB260686E5AEEE19428A3736747453E0A5C0C59B081633C85C7FAC7C3089`。
- 当前安装包已经包含 Codex 临时命令、后台 Git 同步、复制次数和手动次数排序；安装启动、同步期间拖动窗口、复制统计、界面操作、键盘和卸载数据保留由用户在本次新包上手工复验。
- 复制计数保存已经增加 DOM 身份回归门禁，自动验证计数变化时保留原命令卡片、复制按钮、滚动与展开上下文。
