# CommandShelf 安装与手工验收说明

## 发布产物

- 免安装主程序：`C:\Users\Administrator\AppData\Local\Temp\command-shelf-s6-release-target\x86_64-pc-windows-msvc\release\command-shelf.exe`
- Windows x64 NSIS 安装包：`C:\Users\Administrator\AppData\Local\Temp\command-shelf-s6-release-target\x86_64-pc-windows-msvc\release\bundle\nsis\CommandShelf_0.1.0_x64-setup.exe`
- 主程序 SHA-256：`C08FF969148F3BE8C7DF4F151870945B80692B4CFE45E45D30530B5655B97405`
- 安装包 SHA-256：`3D47C12177B2593088280FA26C8F3684DECD07A8B5C1283A529877609C2AF244`

主程序已确认是 Windows x64 PE，体积为 8.572 MiB；安装包体积为 1.886 MiB。连接数据仓库前需要安装 Git for Windows；未安装时应用会给出安装提示。

## 开始前

Codex 会打开免安装主程序，并准备一个空的本地 Git 测试仓库。首次进入后：

1. 点击左侧“仓库”。
2. 输入交付消息中的“测试仓库路径”并连接。
3. 新建一个“验收”分类，再添加三条简单命令，例如 `git status`、`git log -1`、`git branch`。
4. 等界面显示“本地有修改”后开始下面的检查。

## 需要手工完成的检查

### 1. 两种窗口尺寸

分别在约 1440×1024 和 1024×768 的窗口下检查：

- 页面没有横向滚动条、遮挡或内容重叠。
- 仓库、拉取、推送、添加分类和添加命令均可看到并可操作。
- 命令说明、用法、参数说明和参考输出仍然清晰可读。

### 2. 键盘操作

- 打开“仓库”对话框，确认 Tab 与 Shift+Tab 不会跑到背景页面，Escape 能关闭并回到“仓库”按钮。
- 打开“添加命令”或“编辑命令”抽屉，确认焦点先进入标题；Tab 与 Shift+Tab 在抽屉内循环；Escape 关闭后回到原按钮。
- 在第二条命令的排序手柄上按 Alt+↑，确认它上移且焦点仍在同一命令；再按 Alt+↓ 恢复。
- 在第一条按 Alt+↑、最后一条按 Alt+↓，确认顺序不变并出现明确提示。
- 编辑一条命令并保存，确认保存完成后焦点回到该命令的编辑按钮。

### 3. 三次性能采样

每次都先完全退出 CommandShelf，再重新打开免安装主程序并记录：

- 从启动到页面可操作的时间；三次中的最大值目标不高于 1 秒。
- 页面稳定空闲约 5 秒后，CommandShelf 与其 WebView2 子进程的私有内存总量；目标不高于 220 MiB。
- 同一空闲阶段的 CPU；目标不高于 0.5%。

首次启动若触发 WebView2 初始化，可单独注明，不与之后两次混为同一种样本。

### 4. 安装与卸载

1. 退出当前免安装主程序。
2. 运行 NSIS 安装包，完成当前用户安装。
3. 从开始菜单启动 CommandShelf，重新连接测试仓库，确认三条命令仍在。
4. 关闭应用，在 Windows“已安装的应用”中卸载 CommandShelf。
5. 确认应用入口已移除，同时测试仓库目录及其中的 `commands.json` 没有被删除。

## 反馈格式

完成后回复下面六项即可；失败项附一张截图或现象描述：

```text
1440×1024：通过 / 失败
1024×768：通过 / 失败
键盘与焦点：通过 / 失败
三次启动、内存、CPU：填写三组数值
安装：通过 / 失败
卸载且数据保留：通过 / 失败
```
